services:
  - type: web
    name: sponsorship-radar
    runtime: docker
    plan: free            # ~231MB resident; fits the 512MB free tier
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false       # set in the Render dashboard, never committed
